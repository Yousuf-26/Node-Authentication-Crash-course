//HOW TO CREATE COOKIES

/*
Wehen you assign a string to document.cookie, the browser parses it as a cookie and adds it to its list of cookies
syntax
    - document.cookie = "name=value"
    - document.cookie = "name=value; expires=date; domain=domain; path=path; secure"
                            OR based on browser support
    - document.cookie = "name=value; max-age=in-second; domain=domain; path=path; secure"
*/
